# Trigger certification after fixture repair
# Runtime authentication enabled for protected certification
name: V1 Persisted Publication Certification


on:
  workflow_dispatch:
  push:
    branches:
      - v1/persisted-publication-certification
    paths:
      - .github/workflows/v1-persisted-publication-certification.yml


permissions:
  contents: read


concurrency:
  group: v1-persisted-publication-certification
  cancel-in-progress: false


jobs:
  certify-persisted-publication:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    environment: production-certification
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
      MICIRQL_CERTIFICATION_WORKSPACE_ID: ${{ secrets.MICIRQL_CERTIFICATION_WORKSPACE_ID }}
      MICIRQL_CERTIFICATION_ACTOR_ID: ${{ secrets.MICIRQL_CERTIFICATION_ACTOR_ID }}
      MICIRQL_ENABLE_REVISION_CERTIFICATION_ROUTE: "1"
    steps:
      - name: Checkout exact requested head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}


# Trigger exact revision transport fix
