"use client";
import { useEffect,useMemo,useRef,useState } from "react";
import { SCHEMA_VERSION,siteSchema,type Site,type SitePage,type SiteSection } from "@micirql/schema";
import { FAMILY_CODES,SECTION_FAMILIES,sectionDesignId,type SectionFamily,type SectionVariant } from "@micirql/sections";
import { createEditorHistory,createEditorState,executeEditorCommand,markEditorSaved,redoEditor,setEditorSelection,setEditorViewport,undoEditor,type EditorHistory,type EditorViewport } from "@micirql/workspace";
import { RendererPreview } from "./renderer-preview";
import { SectionDesignSwitcher } from "./section-design-switcher";
import { SectionControls } from "./section-controls";
import { ThemeStudio } from "./theme-studio";
import { IndustryDesignPresets } from "./industry-design-presets";
import { AssetPicker } from "./asset-picker";
import { PagesManager } from "./pages-manager";
import { FunctionsManager } from "./functions-manager";
import { PublishReadinessManager,publishReadiness } from "./publish-readiness";
import { PublishController } from "./publish-controller";
import { AiEditorAssistant } from "./ai-editor-assistant";
import { CanvasAddSectionFlow } from "./canvas-add-section-flow";
import type { AiEditorOperation,AiEditorSectionFamily } from "./ai-edit-types";
import type { SupabaseSession } from "./auth-client";

type Mode="content"|"images"|"design"|"pages"|"seo"|"functions"|"domain";
type SaveState="loading"|"saved"|"unsaved"|"saving"|"conflict"|"error";
type DraftApiRecord={workspaceId:string;siteId:string;revision:number;snapshot:Site;updatedAt:string;updatedBy:string};
type GroundingIssue={field?:string;reason?:string;original?:string};

const MODE_LABELS:Record<Mode,string>={content:"Content",images:"Media",design:"Design",pages:"Pages",seo:"SEO",functions:"Functions",domain:"Publish"};
const MODE_HELP:Record<Mode,string>={content:"Edit sections and copy",images:"Replace and manage section imagery",design:"Brand, theme and section variants",pages:"Manage website pages and URLs",seo:"Search appearance for this page",functions:"Connect forms, booking and actions",domain:"Readiness, domain and publishing"};
const MODE_GROUPS:{label:string;items:Mode[]}[]=[{label:"Build",items:["pages","content","images"]},{label:"Style",items:["design"]},{label:"Function",items:["functions"]},{label:"Grow",items:["seo"]},{label:"Publish",items:["domain"]}];
const MOBILE_MODES:Mode[]=["content","images","design","pages","functions","seo","domain"];

const initialSite:Site={schemaVersion:SCHEMA_VERSION,siteId:"workspace-preview",workspaceId:"workspace-demo",name:"Your website",domain:"landing-page",theme:{family:"minimalist",modifiers:["light"],brand:{colors:{primary:"#6d5dfc",secondary:"#171717",accent:"#8b7fff",background:"#ffffff",surface:"#f5f5f7",textPrimary:"#111111",textSecondary:"#65656b",border:"#dddde3",success:"#168a4a",warning:"#ad6a00",error:"#c93636"},typography:{display:"Arial",body:"Arial",ui:"Arial"},density:"comfortable",shape:"balanced",motion:"subtle"}},seoBlueprint:{primaryGoal:"Present the business clearly and convert visitors",targetLocations:[],priorityTopics:[],audiences:[],languages:["en"],localSeo:false,servicePages:true,locationPages:false,blog:false},pages:[{id:"home",path:"/",name:"Home",sections:[{id:"hero-1",component:{componentId:"hero.placeholder",version:"1.0.0"},props:{eyebrow:"Built with MiCirql",heading:"A website your business can grow into.",body:"Select any section to edit its content, images, design and actions without touching code."},bindings:{},hidden:false},{id:"features-1",component:{componentId:"features.placeholder",version:"1.0.0"},props:{heading:"Everything stays editable",body:"The live draft is a validated Site Schema. Changes update the preview immediately and remain safe to publish.",items:[{title:"Mobile first",description:"Every layout begins with the smallest screen."},{title:"Fast by default",description:"Approved components stay within the MiCirql performance protocol."},{title:"Fully editable",description:"Content, assets, design and functionality remain workspace-controlled."}]},bindings:{},hidden:false}],seo:{title:"Your website",description:"A website created with MiCirql.",canonicalPath:"/",indexable:true,structuredDataTypes:[]}}],navigation:[{label:"Home",href:"/"}],integrations:[],domains:[]};

export default function WorkspaceClient({session,workspaceId,siteId}:{session:SupabaseSession;workspaceId:string;siteId:string}){
 const [history,setHistory]=useState<EditorHistory>(()=>createEditorHistory(createEditorState(initialSite)));
 const [mode,setMode]=useState<Mode>("content");
 const [saveState,setSaveState]=useState<SaveState>("loading");
 const [saveError,setSaveError]=useState("");
 const [groundingIssue,setGroundingIssue]=useState<GroundingIssue>();
 const [persistedRevision,setPersistedRevision]=useState(0);
 const [conflictDraft,setConflictDraft]=useState<DraftApiRecord>();
 const [canvasInsert,setCanvasInsert]=useState<{afterSectionId?:string}>();
 const blockedSaveRef=useRef<{snapshot:Site;editorRevision:number}>();
 const loaded=useRef(false);
 const state=history.present;
 const selection=state.selected;
 const selectedPageId=selection.kind==="page"||selection.kind==="section"?selection.pageId:undefined;
 const selectedSectionId=selection.kind==="section"?selection.sectionId:undefined;
 const activePage=useMemo(()=>selectedPageId?state.site.pages.find(p=>p.id===selectedPageId)??state.site.pages[0]!:state.site.pages[0]!,[state.site.pages,selectedPageId]);
 const activeSection=selectedSectionId?activePage.sections.find(s=>s.id===selectedSectionId):undefined;
 const activeSectionFamily=activeSection?sectionFamilyFromComponentId(activeSection.component.componentId):undefined;
 const readiness=publishReadiness(state.site);
 const currentAssetId=activeSection&&typeof activeSection.props.image==="object"&&activeSection.props.image!==null&&!Array.isArray(activeSection.props.image)?String((activeSection.props.image as Record<string,unknown>).assetId??"")||undefined:undefined;
 useEffect(()=>{let cancelled=false;loaded.current=false;blockedSaveRef.current=undefined;setSaveState("loading");setSaveError("");setGroundingIssue(undefined);(async()=>{try{const q=new URLSearchParams({workspaceId,siteId});const r=await fetch(`/api/drafts?${q}`,{headers:{Authorization:`Bearer ${session.access_token}`},cache:"no-store"});if(cancelled)return;if(r.status===404){setSaveError("The website draft could not be found.");setSaveState("error");loaded.current=true;return}if(!r.ok)throw new Error(`Draft load failed (${r.status}).`);const p=await r.json() as {draft:DraftApiRecord};const site=siteSchema.parse(p.draft.snapshot);const editor=createEditorState(site);editor.revision=p.draft.revision;setHistory(createEditorHistory(editor));setPersistedRevision(p.draft.revision);setSaveState("saved");loaded.current=true}catch(e){if(!cancelled){console.error(e);setSaveError(e instanceof Error?e.message:"The draft could not be loaded.");setSaveState("error");loaded.current=true}}})();return()=>{cancelled=true}},[session.access_token,workspaceId,siteId]);
 useEffect(()=>{if(!loaded.current||!state.dirty||saveState==="saving"||saveState==="conflict")return;const t=window.setTimeout(()=>void persist(state.site,state.revision),700);return()=>window.clearTimeout(t)},[state.site,state.revision,state.dirty,persistedRevision,saveState]);
 function commit(command:Parameters<typeof executeEditorCommand>[1]){try{blockedSaveRef.current=undefined;setSaveError("");setGroundingIssue(undefined);setHistory(c=>executeEditorCommand(c,command));setSaveState("unsaved")}catch(e){window.alert(e instanceof Error?e.message:"This change is not allowed.")}}
 function selectPage(pageId:string){setHistory(c=>({...c,present:setEditorSelection(c.present,{kind:"page",pageId})}))}
 function selectSection(pageId:string,sectionId:string){setHistory(c=>({...c,present:setEditorSelection(c.present,{kind:"section",pageId,sectionId})}))}
 function setViewport(viewport:EditorViewport){setHistory(c=>({...c,present:setEditorViewport(c.present,viewport)}))}
 function applyAiOperation(operation:AiEditorOperation){if(operation.type==="page.add"){if(state.site.pages.some(page=>page.path===operation.path)){window.alert("A page with that URL already exists.");return}const id=`${operation.name.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"")||"page"}-${crypto.randomUUID().slice(0,8)}`;const page:SitePage={id,path:operation.path,name:operation.name,sections:[{id:`hero-${crypto.randomUUID().slice(0,8)}`,component:{componentId:sectionDesignId(state.site.theme.family,"hero",2),version:"1.0.0"},props:{heading:operation.name,body:"Add your business-specific message here, then refine it with MiCirql AI or the visual editor."},bindings:{},hidden:false},{id:`about-${crypto.randomUUID().slice(0,8)}`,component:{componentId:sectionDesignId(state.site.theme.family,"about",1),version:"1.0.0"},props:{heading:`About ${state.site.name}`,body:"Use this section to explain the information visitors need on this page."},bindings:{},hidden:false},{id:`cta-${crypto.randomUUID().slice(0,8)}`,component:{componentId:sectionDesignId(state.site.theme.family,"cta",2),version:"1.0.0"},props:{heading:"Ready for the next step?",body:"Choose the action you want visitors to take and connect it in the Functions panel."},bindings:{},hidden:false}],seo:{title:`${operation.name} | ${state.site.name}`,description:`Learn more about ${operation.name.toLowerCase()} from ${state.site.name}.`,canonicalPath:operation.path,indexable:true,structuredDataTypes:[]}};commit({type:"page.add",page,navigationLabel:operation.name});selectPage(id);setMode("content");return}if(operation.type==="section.add"){const id=`${operation.family}-${crypto.randomUUID().slice(0,8)}`;const section:SiteSection={id,component:{componentId:operation.componentId??sectionDesignId(state.site.theme.family,operation.family,operation.variant??2),version:operation.version??"1.0.0"},props:seedSectionProps(operation.family),bindings:{},hidden:false};const selectedIndex=activeSection?activePage.sections.findIndex(item=>item.id===activeSection.id):-1;const toIndex=operation.position==="after-selected"&&selectedIndex>=0?selectedIndex+1:activePage.sections.length;commit({type:"section.add",pageId:activePage.id,section,toIndex});selectSection(activePage.id,id);setMode("content");return}if(operation.type==="seo.patch"){commit({type:"page.seo.patch",pageId:activePage.id,patch:{...(operation.title?{title:operation.title}:{}),...(operation.description?{description:operation.description}:{})}});setMode("seo");return}if(!activeSection){window.alert("Select a section before applying this AI edit.");return}if(operation.type==="media.open"){setMode("images");return}if(operation.type==="functions.open"){setMode("functions");return}if(operation.type==="section.visibility"){commit({type:"section.hidden.set",pageId:activePage.id,sectionId:activeSection.id,hidden:operation.hidden});setMode("content");return}if(operation.type==="section.remove"){commit({type:"section.remove",pageId:activePage.id,sectionId:activeSection.id});selectPage(activePage.id);setMode("content");return}if(operation.type==="section.move"){const index=activePage.sections.findIndex(item=>item.id===activeSection.id);const toIndex=operation.direction==="top"?0:operation.direction==="bottom"?activePage.sections.length-1:operation.direction==="up"?Math.max(0,index-1):Math.min(activePage.sections.length-1,index+1);commit({type:"section.reorder",pageId:activePage.id,sectionId:activeSection.id,toIndex});return}if(!activeSectionFamily){window.alert("This section does not have a recognized MiCirql design family yet.");return}if(operation.type==="section.variant"){commit({type:"section.component.set",pageId:activePage.id,sectionId:activeSection.id,componentId:sectionDesignId(state.site.theme.family,activeSectionFamily,operation.variant),version:activeSection.component.version});if(operation.heading)commit({type:"content.set",pageId:activePage.id,sectionId:activeSection.id,propPath:activeSection.props.title!==undefined?"title":"heading",value:operation.heading});if(operation.body)commit({type:"content.set",pageId:activePage.id,sectionId:activeSection.id,propPath:activeSection.props.description!==undefined?"description":"body",value:operation.body});return}if(operation.type==="section.copy"){if(operation.heading)commit({type:"content.set",pageId:activePage.id,sectionId:activeSection.id,propPath:activeSection.props.title!==undefined?"title":"heading",value:operation.heading});if(operation.body)commit({type:"content.set",pageId:activePage.id,sectionId:activeSection.id,propPath:activeSection.props.description!==undefined?"description":"body",value:operation.body})}}
 async function persist(snapshot:Site,sentEditorRevision:number):Promise<boolean>{if(saveState==="conflict"||saveState==="saving")return false;setSaveState("saving");setSaveError("");setGroundingIssue(undefined);try{const r=await fetch("/api/drafts",{method:"PUT",headers:{Authorization:`Bearer ${session.access_token}`,"content-type":"application/json"},body:JSON.stringify({snapshot,expectedRevision:persistedRevision,updatedBy:"workspace-user"})});const p=await r.json() as {draft?:DraftApiRecord;error?:string;code?:string;groundingIssues?:GroundingIssue[]};if(r.status===409){blockedSaveRef.current=undefined;setConflictDraft(p.draft);setSaveState("conflict");return false}if(!r.ok||!p.draft){if(p.code==="CONTENT_GROUNDING_NOT_READY"){blockedSaveRef.current={snapshot,editorRevision:sentEditorRevision};setGroundingIssue(p.groundingIssues?.[0])}else blockedSaveRef.current=undefined;const message=p.code==="CONTENT_GROUNDING_NOT_READY"?groundingSaveMessage(p.groundingIssues??[]):p.error??`Draft save failed (${r.status}).`;throw new Error(message)}blockedSaveRef.current=undefined;setPersistedRevision(p.draft.revision);setHistory(c=>({...c,present:c.present.revision===sentEditorRevision?markEditorSaved(c.present):c.present}));setSaveError("");setGroundingIssue(undefined);setSaveState(c=>c==="conflict"?c:"saved");return true}catch(e){console.error(e);setSaveError(e instanceof Error?e.message:"The latest change could not be saved.");setSaveState("error");return false}}
 function openGroundingFact(){if(!groundingIssue)return;setMode("domain");window.setTimeout(()=>{window.dispatchEvent(new CustomEvent("micirql:add-grounding-fact",{detail:groundingIssue}));document.querySelector<HTMLElement>(".business-facts-editor")?.scrollIntoView({behavior:"smooth",block:"start"})},0)}
 function retryBlockedSave(){const blocked=blockedSaveRef.current;if(!blocked)return;if(state.revision!==blocked.editorRevision){blockedSaveRef.current=undefined;setSaveError("Verified facts were saved, but the page changed after the blocked edit. Save the current version normally so MiCirql validates the latest copy.");setGroundingIssue(undefined);setSaveState("unsaved");return}void persist(blocked.snapshot,blocked.editorRevision)}
 async function ensureSaved(){if(!state.dirty&&saveState==="saved")return true;return persist(state.site,state.revision)}
 async function leaveEditor(){if(saveState==="conflict"){if(!window.confirm("This draft has a save conflict. Leaving now may discard local edits. Leave editor anyway?"))return;window.location.href="/";return}if(state.dirty||saveState==="unsaved"||saveState==="error"){const saved=await ensureSaved();if(!saved&&!window.confirm("Your latest changes could not be saved. Leave editor anyway?"))return}window.location.href="/"}
 function loadServerVersion(){if(!conflictDraft)return;blockedSaveRef.current=undefined;const site=siteSchema.parse(conflictDraft.snapshot);const editor=createEditorState(site);editor.revision=conflictDraft.revision;setHistory(createEditorHistory(editor));setPersistedRevision(conflictDraft.revision);setConflictDraft(undefined);setSaveError("");setGroundingIssue(undefined);setSaveState("saved")}
 return <main className="workspace-shell editor-v2"><header className="workspace-topbar"><div className="editor-brand-lockup"><button type="button" className="editor-back-button" onClick={()=>void leaveEditor()} aria-label="Back to projects">← <span>Projects</span></button><div className="editor-project-title"><strong>MiCirql</strong><span>{state.site.name}</span></div></div><div className="page-switcher" aria-label="Pages">{state.site.pages.map(p=><button key={p.id} className={p.id===activePage.id?"is-active":""} onClick={()=>selectPage(p.id)}>{p.name}</button>)}</div><div className="workspace-actions"><span className={`save-state ${saveState}`}>{saveLabel(saveState)}</span><button className="editor-icon-button" title="Undo" onClick={()=>{blockedSaveRef.current=undefined;setSaveError("");setGroundingIssue(undefined);setHistory(c=>undoEditor(c));setSaveState("unsaved")}} disabled={!history.past.length}>↶</button><button className="editor-icon-button" title="Redo" onClick={()=>{blockedSaveRef.current=undefined;setSaveError("");setGroundingIssue(undefined);setHistory(c=>redoEditor(c));setSaveState("unsaved")}} disabled={!history.future.length}>↷</button><button className="editor-save-button" onClick={()=>void persist(state.site,state.revision)} disabled={saveState==="saving"||saveState==="loading"}>Save</button><PublishController site={state.site} disabled={!readiness.ready||saveState==="loading"||saveState==="conflict"} ensureSaved={ensureSaved}/></div></header>{saveError?<section className="draft-conflict" role="alert"><div><strong>That change was not saved.</strong><span>{saveError}</span></div>{groundingIssue?<button type="button" onClick={openGroundingFact}>Add to verified facts</button>:null}</section>:null}{saveState==="conflict"?<section className="draft-conflict" role="alert"><div><strong>This draft changed elsewhere.</strong><span>Your local edits were not overwritten.</span></div><button onClick={loadServerVersion}>Load latest saved version</button></section>:null}<div className="workspace-main"><aside className="workspace-rail" aria-label="Editor tools"><div className="rail-heading"><span>Editor</span><strong>Build visually</strong></div>{MODE_GROUPS.map(group=><div className="rail-group" key={group.label}><span className="rail-group-label">{group.label}</span>{group.items.map(item=><button key={item} className={mode===item?"is-active":""} onClick={()=>setMode(item)}><span className="rail-tool-icon" aria-hidden="true">{modeIcon(item)}</span><span><strong>{MODE_LABELS[item]}</strong><small>{MODE_HELP[item]}</small></span></button>)}</div>)}</aside><section className="workspace-canvas"><div className="canvas-toolbar"><div className="canvas-context"><span>{activePage.name}</span><strong>{activeSection?sectionDisplayName(activeSection.component.componentId):"Page preview"}</strong></div><div className="viewport-switcher" aria-label="Preview size">{(["mobile","tablet","desktop"] as EditorViewport[]).map(v=><button key={v} className={state.viewport===v?"is-active":""} onClick={()=>setViewport(v)}>{viewportLabel(v)}</button>)}</div></div><div className="canvas-stage">{canvasInsert?<CanvasAddSectionFlow site={state.site} pageId={activePage.id} afterSectionId={canvasInsert.afterSectionId} onCancel={()=>setCanvasInsert(undefined)} onChoose={(family,candidate)=>{const id=`${family}-${crypto.randomUUID().slice(0,8)}`;const section:SiteSection={id,component:{componentId:candidate.componentId,version:candidate.version},props:seedSectionProps(family),bindings:{},hidden:false};const anchor=canvasInsert.afterSectionId;const anchorIndex=anchor?activePage.sections.findIndex(item=>item.id===anchor):-1;const toIndex=anchor?Math.max(0,anchorIndex+1):0;commit({type:"section.add",pageId:activePage.id,section,toIndex});setCanvasInsert(undefined);selectSection(activePage.id,id);setMode("content")}}/>:null}<RendererPreview site={state.site} path={activePage.path} viewport={state.viewport} {...(activeSection?{selectedSectionId:activeSection.id}:{})} onSelectSection={id=>selectSection(activePage.id,id)} onInlineTextChange={(sectionId,propPath,value)=>commit({type:"content.set",pageId:activePage.id,sectionId,propPath,value})} onRequestImageChange={(sectionId)=>{selectSection(activePage.id,sectionId);setMode("images")}} onRequestDesignChange={(sectionId)=>{selectSection(activePage.id,sectionId);setMode("design")}} onRequestCopyEdit={(sectionId)=>{selectSection(activePage.id,sectionId);setMode("content")}} onRequestMove={(sectionId,direction)=>{const index=activePage.sections.findIndex(item=>item.id===sectionId);if(index<0)return;const toIndex=direction==="up"?Math.max(0,index-1):Math.min(activePage.sections.length-1,index+1);commit({type:"section.reorder",pageId:activePage.id,sectionId,toIndex})}} onRequestVisibility={(sectionId,hidden)=>{selectSection(activePage.id,sectionId);commit({type:"section.hidden.set",pageId:activePage.id,sectionId,hidden})}} onRequestAddSection={(afterSectionId)=>setCanvasInsert({...(afterSectionId?{afterSectionId}:{})})} onReorderSection={(sectionId,toIndex)=>commit({type:"section.reorder",pageId:activePage.id,sectionId,toIndex})}/></div></section><aside className="workspace-inspector"><div className="inspector-heading"><div><span>{MODE_LABELS[mode]}</span><strong>{activeSection?sectionDisplayName(activeSection.component.componentId):activePage.name}</strong></div><small>{MODE_HELP[mode]}</small></div><AiEditorAssistant site={state.site} pageId={activePage.id} {...(activeSection?{sectionId:activeSection.id}:{})} onApply={applyAiOperation}/>{activeSection?<div className="context-quick-actions" aria-label="Section quick actions"><button className={mode==="content"?"is-active":""} onClick={()=>setMode("content")}>Edit copy</button><button className={mode==="images"?"is-active":""} onClick={()=>setMode("images")}>Image</button><button className={mode==="design"?"is-active":""} onClick={()=>setMode("design")}>Design</button><button className={mode==="functions"?"is-active":""} onClick={()=>setMode("functions")}>Action</button></div>:null}{mode==="content"?<><SectionControls page={activePage} {...(activeSection?{selectedSectionId:activeSection.id}:{})} onSelect={id=>selectSection(activePage.id,id)} onAdd={section=>{commit({type:"section.add",pageId:activePage.id,section});selectSection(activePage.id,section.id)}} onMove={(sectionId,toIndex)=>commit({type:"section.reorder",pageId:activePage.id,sectionId,toIndex})} onToggleHidden={(sectionId,hidden)=>commit({type:"section.hidden.set",pageId:activePage.id,sectionId,hidden})} onRemove={sectionId=>{commit({type:"section.remove",pageId:activePage.id,sectionId});selectPage(activePage.id)}}/>{activeSection?<div className="inspector-form"><label>Heading<input value={String(activeSection.props.heading??activeSection.props.title??"")} onChange={e=>commit({type:"content.set",pageId:activePage.id,sectionId:activeSection.id,propPath:activeSection.props.title!==undefined?"title":"heading",value:e.target.value})}/></label><label>Body<textarea value={String(activeSection.props.body??activeSection.props.description??"")} onChange={e=>commit({type:"content.set",pageId:activePage.id,sectionId:activeSection.id,propPath:activeSection.props.description!==undefined?"description":"body",value:e.target.value})}/></label></div>:<div className="inspector-empty"><p>Select a section in the preview to edit it.</p></div>}</>:mode==="images"&&activeSection&&activeSectionFamily?<AssetPicker workspaceId={state.site.workspaceId} siteId={state.site.siteId} pagePath={activePage.path} sectionId={activeSection.id} domain={state.site.domain} theme={state.site.theme.family} family={activeSectionFamily} {...(currentAssetId?{currentAssetId}:{})} onSelect={(asset,o)=>commit({type:"asset.set",pageId:activePage.id,sectionId:activeSection.id,propPath:"image",asset:{assetId:asset.id,alt:o?.alt??asset.alt,focalPoint:o?.focalPoint??asset.focalPoint}})}/>:mode==="pages"?<PagesManager site={state.site} activePageId={activePage.id} onSelect={selectPage} onAdd={p=>{commit({type:"page.add",page:p});selectPage(p.id)}} onRemove={id=>{commit({type:"page.remove",pageId:id});if(id===activePage.id)selectPage(state.site.pages[0]!.id)}} onDuplicate={p=>{commit({type:"page.add",page:p});selectPage(p.id)}} onPathChange={(id,path)=>commit({type:"page.path.set",pageId:id,path})} onReorder={(id,toIndex)=>commit({type:"page.reorder",pageId:id,toIndex})}/>:mode==="functions"&&activeSection?<FunctionsManager site={state.site} section={activeSection} onBind={(bindingKey,actionId)=>commit({type:"binding.set",pageId:activePage.id,sectionId:activeSection.id,bindingKey,actionId})} onRemove={bindingKey=>commit({type:"binding.remove",pageId:activePage.id,sectionId:activeSection.id,bindingKey})} onSetPrimaryAction={(href,label)=>{commit({type:"content.set",pageId:activePage.id,sectionId:activeSection.id,propPath:"primaryAction.href",value:href});commit({type:"content.set",pageId:activePage.id,sectionId:activeSection.id,propPath:"primaryAction.label",value:label})}}/>:mode==="domain"?<PublishReadinessManager site={state.site} onFactsSaved={retryBlockedSave}/>:mode==="seo"?<div className="inspector-form"><label>SEO title<input value={activePage.seo.title} onChange={e=>commit({type:"page.seo.patch",pageId:activePage.id,patch:{title:e.target.value}})}/></label><label>Description<textarea value={activePage.seo.description} onChange={e=>commit({type:"page.seo.patch",pageId:activePage.id,patch:{description:e.target.value}})}/></label></div>:mode==="design"?<div className="inspector-form"><IndustryDesignPresets onApply={preset=>{const components=state.site.pages.flatMap(page=>page.sections.flatMap(section=>{const family=sectionFamilyFromComponentId(section.component.componentId);if(!family)return[];const variant=preset.variants[family]??sectionVariantFromComponentId(section.component.componentId);return[{pageId:page.id,sectionId:section.id,componentId:sectionDesignId(preset.theme.family,family,variant),version:section.component.version}]}));commit({type:"design.preset.apply",theme:preset.theme,components})}}/><ThemeStudio theme={state.site.theme} onChange={theme=>commit({type:"theme.set",theme})}/>{activeSection&&activeSectionFamily?<SectionDesignSwitcher site={state.site} pageId={activePage.id} section={activeSection} family={activeSectionFamily} theme={state.site.theme.family} currentComponentId={activeSection.component.componentId} onSelect={(componentId,version)=>commit({type:"section.component.set",pageId:activePage.id,sectionId:activeSection.id,componentId,version})}/>:null}</div>:<div className="inspector-empty"><p>{mode==="images"?"Select a section with an image to manage its media.":mode==="functions"?"Select a section to connect functionality.":"Select a section to configure this area."}</p></div>}</aside></div><nav className="mobile-editor-bar" aria-label="Editor tools">{MOBILE_MODES.map(item=><button key={item} className={mode===item?"is-active":""} onClick={()=>setMode(item)}><span>{modeIcon(item)}</span><small>{MODE_LABELS[item]}</small></button>)}</nav></main>;
}
function seedSectionProps(family:AiEditorSectionFamily):SiteSection["props"]{if(family==="about")return{heading:"About us",body:"Add the business story, approach and details visitors should understand here."};if(family==="services")return{heading:"Services",body:"Introduce the services that matter most to your visitors.",items:[{title:"Service one",description:"Add a clear service description."},{title:"Service two",description:"Add a clear service description."},{title:"Service three",description:"Add a clear service description."}]};if(family==="features")return{heading:"Why choose us",items:[{title:"Benefit one",description:"Explain a real customer benefit."},{title:"Benefit two",description:"Explain a real customer benefit."},{title:"Benefit three",description:"Explain a real customer benefit."}]};if(family==="process")return{heading:"How it works",items:[{title:"Step one",description:"Describe the first step."},{title:"Step two",description:"Describe the next step."},{title:"Step three",description:"Describe what happens next."}]};if(family==="testimonials")return{heading:"What customers say",items:[]};if(family==="gallery")return{heading:"Gallery",items:[]};if(family==="team")return{heading:"Meet the team",items:[]};if(family==="cta")return{heading:"Ready for the next step?",body:"Connect the right action for this page in the Functions panel."};return{heading:"Contact us",body:"Add the contact details and enquiry action visitors should use."};}
function groundingSaveMessage(issues:GroundingIssue[]){const issue=issues[0];if(!issue)return"This edit contains a factual claim that is not in your verified business brief. Remove the claim or add the verified fact to your business details before saving.";const claim=issue.original?.trim();const reason=issue.reason?.trim();return`${claim?`“${claim}” `:"This claim "}was not saved${reason?` because it is an ${reason.replace(/^unsupplied /,"unverified ").replace(/^unsupported /,"unverified ")}`:" because it is not in your verified business brief"}. Remove it, or add the verified fact to your business details first.`}
function sectionFamilyFromComponentId(componentId:string):SectionFamily|undefined{const n=componentId.toLowerCase();const legacy=SECTION_FAMILIES.find(f=>n===`${f}.placeholder`||n.startsWith(`${f}.`));if(legacy)return legacy;const u=componentId.toUpperCase();return SECTION_FAMILIES.find(f=>u.includes(`-${FAMILY_CODES[f]}-`))}
function sectionVariantFromComponentId(componentId:string):SectionVariant{const match=componentId.match(/-(00[1-5])$/);const value=match?Number(match[1]):1;return value>=1&&value<=5?value as SectionVariant:1}
function saveLabel(s:SaveState){return s==="loading"?"Loading…":s==="saving"?"Saving…":s==="unsaved"?"Unsaved":s==="conflict"?"Conflict":s==="error"?"Save error":"Saved"}
function viewportLabel(v:EditorViewport){return v==="mobile"?"Mobile":v==="tablet"?"Tablet":"Desktop"}
function sectionDisplayName(componentId:string){const family=sectionFamilyFromComponentId(componentId);if(family)return family.charAt(0).toUpperCase()+family.slice(1);return componentId.split(".")[0]?.replace(/[-_]/g," ")||"Section"}
function modeIcon(mode:Mode){return mode==="content"?"✎":mode==="images"?"▧":mode==="design"?"◐":mode==="pages"?"▤":mode==="seo"?"⌕":mode==="functions"?"⚡":"↑"}