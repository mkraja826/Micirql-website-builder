export function EditorialInquiryContact({ eyebrow, headline, body, status, submitLabel, note }: { eyebrow:string; headline:string; body:string; status:string; submitLabel:string; note:string }) {
  return <section className="shell" id="contact"><div style={{display:"grid",gap:"2rem",gridTemplateColumns:"repeat(auto-fit,minmax(18rem,1fr))"}}><div><p className="eyebrow">{eyebrow}</p><h2>{headline}</h2><p>{body}</p><p><strong>{status}</strong></p></div><form style={{display:"grid",gap:"1rem"}}><label>Name<input name="name" placeholder="Your name" /></label><label>Email<input name="email" type="email" placeholder="you@example.com" /></label><label>What would you like to discuss?<textarea name="message" rows={5} placeholder="Share a little context" /></label><button className="button" type="button">{submitLabel}</button><small>{note}</small></form></div></section>;
}
