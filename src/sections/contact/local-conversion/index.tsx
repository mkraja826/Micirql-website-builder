export function LocalConversionContact({ eyebrow, headline, body, note }: { eyebrow:string; headline:string; body:string; note:string }) {
  return <section className="contact shell" id="contact"><div className="contactIntro"><p className="eyebrow">{eyebrow}</p><h2>{headline}</h2><p>{body}</p><span className="contactStatus">Appointment requests are enquiries until confirmed.</span></div><form><label>Name<input name="name" placeholder="Your name"/></label><label>Phone<input name="phone" inputMode="tel" placeholder="Your phone number"/></label><label>What can we help with?<textarea name="message" rows={4} placeholder="Tell us briefly"/></label><button className="button" type="button">Send appointment enquiry</button><small>{note}</small></form></section>;
}
