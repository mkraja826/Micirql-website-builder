export type HeroTrustItem = { label: string };
type Media = { src: string; alt: string; focalPoint?: { x: number; y: number } };

export function EditorialSplitHero({ brand, eyebrow, headline, body, primaryCta, secondaryCta, trustItems, visualNote, media }: { brand:string; eyebrow:string; headline:string; body:string; primaryCta:{label:string; href:string}; secondaryCta:{label:string; href:string}; trustItems:HeroTrustItem[]; visualNote:string; media?:Media }) {
  return <section className="hero shell" id="top"><div className="heroCopy"><p className="eyebrow">{eyebrow}</p><h1>{headline}</h1><p className="lede">{body}</p><div className="actions"><a className="button" href={primaryCta.href}>{primaryCta.label}</a><a className="textLink" href={secondaryCta.href}>{secondaryCta.label} <span>↘</span></a></div><div className="trustLine">{trustItems.map((item)=><span key={item.label}>{item.label}</span>)}</div></div><div className="heroVisual" aria-label={`${brand} visual`}>{media?<img src={media.src} alt={media.alt} style={{position:"absolute",inset:0,width:"100%",height:"100%",objectFit:"cover",objectPosition:`${(media.focalPoint?.x ?? .5)*100}% ${(media.focalPoint?.y ?? .5)*100}%`}}/>:<><div className="heroFrame"/><div className="arch archOne"/><div className="arch archTwo"/><div className="pearlOrb"/></>}<div className="visualKicker">{brand}</div><div className="visualNote">{visualNote}</div></div></section>;
}
